#include "types.ht"

& decode : export proc(f2d?chan f2d_type & m2d?chan m2d_type & 
                       w2d?chan w2d_type &
                       d2f!chan d2f_type & d2e!chan d2e_type) . 
begin
|
   forever do
   od
end
